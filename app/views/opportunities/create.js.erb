<% if @opportunity.errors[:already_posted_opportunity].present? %>
	$("#duplicated_message").html('<%= j render partial: "duplicated_opportunity_message", :locals => {:message => @opportunity.errors.messages[:already_posted_opportunity].first} %>');
<%end%>

