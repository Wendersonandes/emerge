<% if has_entries? %>
	<li class="list-group-item pb0 bg-white">
		<h3 class="f4 fw3 mt0"> <span class="glyphicon glyphicon-send"></span> <%= model_name_formatted %></h3>
		<%= cell(Components::Opportunity::EntryCell, :collection => model).(:entry) %>
	</li>
<% end %>
