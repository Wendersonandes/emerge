<%= form_for @new_comment, :remote => true do |f| %>
	<%= f.hidden_field :commentable_id, :value => @new_comment.commentable_id %>
	<%= f.hidden_field :commentable_type, :value => @new_comment.commentable_type %>
	<div class="field form-group">
		<%= f.text_area :body, :class => 'form-control', :rows => 3, :placeholder => "Comente aqui" %>
	</div>
	<div class="field form-group">
		<div class="btn-group">
			<%= f.submit "Comentar", :class =>  "button button-rounded button-primary button-small" %>
		</div>
		<div class="btn-group">
			<%= submit_tag("Limpar comentário", { :name => 'reset', :id => 'reset_button', :type => "reset", :class => "button button-small ml1" }) %>
		</div>
	</div>
<% end %>
